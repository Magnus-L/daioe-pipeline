# data is symlinked to the read-only Stata source and produced outputs — never commit
data/
reports/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.ipynb_checkpoints/

# regenerable batch payloads (rebuild with --dry-run)
mapping/mod_data/batch_requests_*.jsonl
